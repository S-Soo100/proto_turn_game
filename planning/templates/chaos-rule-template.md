# {변칙 룰 이름}

| 항목 | 값 |
|---|---|
| **ID** | CR-{SCOPE}{NNN} |
| **게임** | gonggi |
| **상태** | draft / ready / done |
| **생성일** | YYYY-MM-DD |
| **수정일** | YYYY-MM-DD |

---

## 요약

> 이 변칙 룰이 무엇인지 1문장으로 설명

## 발동 조건

| 항목 | 값 |
|---|---|
| **최소 라운드** | (몇 라운드부터 발동 가능한지) |
| **발동 타이밍** | 던진 직후 / 집기 시도 시 / 성공 직전 / 단계 전환 시 |
| **발동 확률** | (기본 확률, 라운드에 따라 가중) |
| **중복 발동** | 가능 / 불가 (같은 라운드에서 다시 발동?) |

## 연출

### 시각 효과
- (어떤 애니메이션/이펙트가 보이는지)

### 사운드 (향후)
- (어떤 효과음이 나는지)

### 타이밍
| 단계 | 시간 | 설명 |
|---|---|---|
| 예고 | ~ms | (발동 직전 힌트/전조) |
| 메인 연출 | ~ms | (핵심 애니메이션) |
| 결과 | ~ms | (결과 표시 + 게임 복귀) |

## 게임 영향

- **돌 상태 변화**: (돌이 어떻게 되는지 — 사라짐/위치 변경/상태 변경)
- **단계 영향**: (현재 단계 실패? 단계 리셋? 그대로?)
- **점수 영향**: (점수 감소? 보너스?)
- **후속 효과**: (다음 턴에 영향이 있는지)

## 구현 가이드

### 데이터 구조
```typescript
{
  id: 'CR-{SCOPE}{NNN}',
  name: '{이름}',
  trigger: '{발동 타이밍}',
  minRound: {숫자},
  probability: {0~1},
  execute: (state: GonggiState) => ChaosResult,
}
```

### 필요한 애니메이션
- (Framer Motion / CSS keyframes / matter.js 중 어떤 것으로 구현할지)

### 참고 패턴
- (기존 코드에서 참고할 수 있는 유사 패턴)

## 열받음 지수

| 항목 | 값 |
|---|---|
| **열받음 정도** | ★☆☆☆☆ ~ ★★★★★ |
| **웃김 정도** | ★☆☆☆☆ ~ ★★★★★ |
| **예측 가능성** | 높음 / 중간 / 낮음 |

## 메모

추가 아이디어, 논의 사항 등
